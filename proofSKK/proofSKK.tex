\documentclass {article}

\usepackage{amsmath}

\begin{document}

\begin{align*}
(SKK)x &= (\lambda x y z.x z (y z)) K K x = Kx (Kx) = (\lambda x y.x)x (Kx) \\
&= x = (\lambda x.x)x = Ix  \implies (SKK) = I
\end{align*}

\end{document}